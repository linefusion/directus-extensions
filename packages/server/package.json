{
  "private": true,
  "name": "@linefusion/directus-server",
  "version": "1.0.1",
  "scripts": {
    "dev": "pnpm bootstrap && nodemon --ignore dist --ignore node_modules ./server.js start",
    "start": "nodemon --ignore dist --ignore node_modules ./server.js start",
    "bootstrap": "pnpm cli bootstrap",
    "cli": "node ./server.js"
  },
  "dependencies": {
    "@directus/api": "12.1.3",
    "@linefusion/directus-blurhash-extractor": "workspace:*",
    "@linefusion/directus-text-extractor": "workspace:*",
    "@linefusion/directus-thumbhash-extractor": "workspace:*",
    "@wolfpkgs/core": "^0.2.13",
    "directus": "10.5.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.3",
    "vitest": "^0.34.6"
  }
}
